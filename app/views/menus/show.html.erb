  
  <div class="innerpage_banner">


  <% essence = Alchemy::EssenceMenu2.find_by(menu_id: menu.id)%>
  <% if !essence.nil? %>

    <center><%= render_elements from_page: 'menu_deux'%></center>
      

<%elsif essence.nil? %>
  <% essence = Alchemy::EssenceMenu.find_by(menu_id: menu.id)%>
    <% if !essence.nil? %>
      <center><%= render_elements from_page: 'menu'%></center>
      <%end%>
<%end%>

    </div>
