<head>
<%= csrf_meta_tags %>
</head>
<%= form_for [:admin, @restaurant], url: main_app.admin_restaurant_path do |f| %>

        <%= f.label :name %>
        <%= f.text_field :name %>

        <%= f.label :city %>
        <%= f.text_field :city %>
    <%= f.collection_select :restaurant_group_id, @restaurant_groups, :id, :name%>

   




      <%= f.submit %>
<%end%>

